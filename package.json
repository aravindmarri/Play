{
  "name": "play",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The PLAY parent gallery and its independently developed browser games.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "validate": "node scripts/build-games.mjs --validate",
    "build": "node scripts/build-games.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
